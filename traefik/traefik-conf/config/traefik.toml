# Production TLS hardening: enforce TLS 1.2+ and a modern cipher suite
[tls.options]
  [tls.options.default]
    minVersion = "VersionTLS12"
    # sniStrict = true  # reject requests whose SNI has no matching certificate
    cipherSuites = [
      "TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256",
      "TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256",
      "TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384",
      "TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384",
      "TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305",
      "TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305",
    ]

# [[tls.certificates]]
#   certFile = "/traefik-conf/certs/cloudflare/tls.crt"
#   keyFile = "/traefik-conf/certs/cloudflare/tls.key"

# [tls.options]
#   [tls.options.default]
#     [tls.options.default.clientAuth]
#       # in PEM format. each file can contain multiple CAs.
#       caFiles = ["/traefik-conf/certs/cloudflare/cacerts.pem"]
#       clientAuthType = "RequireAndVerifyClientCert"